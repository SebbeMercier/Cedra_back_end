import type { Request } from "express";
import type { Auth, MinimalRequest } from "lucia-auth";
export declare const convertExpressRequestToStandardRequest: (req: Request, auth: Auth) => MinimalRequest;
