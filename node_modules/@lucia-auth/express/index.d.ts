import type { Request, Response, NextFunction } from "express";
import type { Auth } from "lucia-auth";
export declare const handleMiddleware: (auth: Auth) => (req: Request, res: Response, next: NextFunction) => Promise<void>;
